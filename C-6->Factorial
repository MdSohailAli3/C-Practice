#include<stdio.h>
int fact(int no);
int main()
{
  int n,z;
  printf("\nenter a no");
scanf("%d",&n);
z=fact(n);
printf("\nfactorial of a no is=%d",z);
return 0;
}
int fact(int no)
{int factorial=1;
 if(no==0)
 { return 1;}
 else{factorial=no*fact(no-1);
 return factorial;

 }
}
